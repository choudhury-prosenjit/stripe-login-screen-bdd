{
  "name": "stripe-login-screen-bdd",
  "version": "1.0.0",
  "description": "BDD Automation for Stripe Dashboard Login using Cucumber + Playwright + TypeScript",
  "scripts": {
    "test": "cucumber-js",
    "test:happy": "cucumber-js --tags @happy-path",
    "test:validation": "cucumber-js --tags @validation",
    "test:navigation": "cucumber-js --tags @navigation",
    "test:security": "cucumber-js --tags @security"
  },
  "dependencies": {
    "@cucumber/cucumber": "^10.3.1",
    "@playwright/test": "^1.44.0",
    "playwright": "^1.44.0",
    "dotenv": "^16.4.5",
    "multiple-cucumber-html-reporter": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7"
  }
}